import { TransferResponceInterceptorInterceptor } from './transfer-responce-interceptor.interceptor';

describe('TransferResponceInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TransferResponceInterceptorInterceptor()).toBeDefined();
  });
});
